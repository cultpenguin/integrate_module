Using github pages to host files as documented here:
https://coderefinery.github.io/documentation/gh_workflow/

To compile locally: 

    sudo apt install pandoc
    pip install --upgrade -r requirements.txt

